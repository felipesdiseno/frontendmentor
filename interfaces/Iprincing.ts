interface IPrincing {
  pack: string;
  priceMonth: string;
  priceYear: string;
  description: string;
  storage: string;
  users: string;
  send: string;
  key?: string;
  pricing: string[];
}
export default IPrincing;
